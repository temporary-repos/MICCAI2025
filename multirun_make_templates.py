import os
import concurrent.futures

import json

# Load in base JSON template
base_template = json.load(open('config/test.json'))

models = [

]

seeds = [
    
]

datasets = [

]

print(base_template)